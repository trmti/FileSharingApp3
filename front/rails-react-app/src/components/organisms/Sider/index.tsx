import { VFC } from 'react';
import { Menu } from 'antd';

type Props = {
  icon: string;
  text: string;
  to: string;
}[];

const Sider: VFC<Props> = (prop) => {
  return <></>;
};

export default Sider;
